import 'package:flutter/material.dart';

class EditRestaurantPhotos extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Edit Restaurant Photos'),
      ),
      body: Center(
        child: Text(
          'edit restaurant photos page',
        ),
      ),
    );
  }
}
